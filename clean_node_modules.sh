#!/bin/bash

echo "Removing all node_modules except the root one"
find ./packages -name "node_modules" -exec rm -rf '{}' +
echo "Removing all package-lock.json except the root one"
find ./packages -name "package-lock.json" -exec rm '{}' +
echo "Done"
