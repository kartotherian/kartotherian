# Number of worker processes to spawn.
# Set to 0 to run everything in a single process without clustering.
# Use 'ncpu' to run as many workers as there are CPU units
num_workers: 0

# Log error messages and gracefully restart a worker if v8 reports that it
# uses more heap (note: not RSS) than this many mb.
worker_heap_limit_mb: 250

# Logger info
logging:
  level: trace
#  streams:
#  # Use gelf-stream -> logstash
#  - type: gelf
#    host: logstash1003.eqiad.wmnet
#    port: 12201

# Statsd metrics reporter
metrics:
  type: info
  #host: localhost
  #port: 8125

services:
  - name: service-template-node
    # a relative path or the name of an npm package, if different from name
    module: ./app.js
    # optionally, a version constraint of the npm package
    # version: ^0.4.0
    # per-service config
    conf:
      port: 4000
      interface: localhost

      # TODO: This does not work, not sure of the exact reqs here, manually disabling for now
      csp: default-src 'self'; script-src 'self' 'unsafe-inline';

      sources:
        v1:
          uri: file://./vectors?safe=true&filetype=pbf
        v1gen:
          uri: bridge://./map/osm-bright.tm2source/data.xml
          enabled: false
        v1auto:
          # Uses other sources, must be last in the list
          uri: autogen://?store=v1&generator=v1gen&saveafter=8&minzoom=0&maxzoom=16
      styles:
        s1:
          tm2: ./map/osm-bright.tm2/project.xml
          source: v1
