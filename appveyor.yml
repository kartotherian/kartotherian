os: Visual Studio 2015

environment:
  matrix:
    - nodejs_version: 0.10.40
    - nodejs_version: 0.12.7

platform:
  - x64
  - x86

install:
  - scripts\build-appveyor.bat

build: OFF
test: OFF
deploy: OFF
